import { FC } from 'react';

// Styles
import './styles.scss';

const Header: FC = () => {
  return (
    <header className='header'>
      <h1>Rick and Morty App</h1>
    </header>
  );
};

export default Header;
